# Acceptance status

T06 live DeepSeek OOB repair passed. T07 four-tool clean/fault pairs passed, including a
fixed five-run racecheck detection rate of 5/5. T09 real strict verification passed all four
sanitizers over one public and thirteen private inputs. T10 registered four live mutation
families. Unit/integration counts are generated by the current checkout and must not be
copied into a release manifest after code changes.

The zero-cost release foundation is implemented: release claims are checked against a
separately derived index of completed, hash-verified public/evaluator RunStore artifacts.
Zero tests, arbitrary run-ID strings, stale commits/configuration, incomplete schedules or
records, cross-split identities, and a release marker that selects only the lightweight gate
are rejected. The offline e2e assertion passes by proving that the current missing manifest
keeps the gate closed; `--require-live` turns the same absence into a failure.

Release remains blocked by all of the following real work:

- expand and GPU-validate 16 public cases (at least four per tool family) and 8 private cases;
- generate evaluator-only mappings from opaque public evaluation aliases to private
  case/template identities (the current runner does not yet produce this artifact);
- freeze an auditable pricing bound and obtain an explicit user-approved API ceiling;
- execute and persist the complete 24×5×3 = 360-unit batch with no missing mode/repeat/case;
- rerun the selected release surface and freeze its JUnit report on the same commit;
- only then generate `evaluation/release-manifest.json` from those artifacts.

No current-commit release manifest or run-report fixture is claimed or fabricated.

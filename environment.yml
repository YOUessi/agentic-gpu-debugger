name: agentic-gpu-debugger
channels:
  - nvidia/label/cuda-12.8.1
  - defaults
dependencies:
  - python=3.11
  - pip
  # Compiler + development runtime + Sanitizer, not the runtime-only package.
  - cuda-nvcc=12.8.93
  - cuda-cudart-dev=12.8.90
  - cuda-sanitizer-api=12.8.93
